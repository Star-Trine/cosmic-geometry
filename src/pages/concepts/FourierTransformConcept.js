import './FourierTransformConcept.css';

export default function FourierTransformConcept() {
  return (
    <main className="fourier-transform-concept">
      <h1>フーリエ変換（FFT）</h1>
      <p>
        このページでは、作品の設計思想、コンセプト、数学的背景、
        使用技術、実装方法、制作メモなどをまとめます。
      </p>
    </main>
  );
}
